import { useState } from 'react'

import { ChannelTitleElement } from './ChannelTitle.styles'

const ChannelTitle = (title) => {

    return (
        <ChannelTitleElement>
            {/* {title} */}
            Le salon
        </ChannelTitleElement>
    )
}

export default ChannelTitle